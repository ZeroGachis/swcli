name: Release Please

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  release-please:
    uses: ZeroGachis/.github/.github/workflows/gh-release-please.yml@v6
    secrets: inherit
    permissions:
      contents: write
      pull-requests: write
    with:
      major_and_minor_tags: false
