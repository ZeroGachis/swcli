name: Release Please

on:
  push:
    branches:
      - main

jobs:
  release-please:
    uses: ZeroGachis/.github/.github/workflows/gh-release-please.yml@v6
    permissions:
      contents: write
      pull-requests: write
    with:
      major_and_minor_tags: false
